# Cloudflare Pages — Zakati is deployed as a static site.
# This file is intentionally minimal because no Functions/Workers are used.

name = "zakati"
compatibility_date = "2024-12-16"
pages_build_output_dir = "apps/web/out"
